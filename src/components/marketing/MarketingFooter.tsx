import { SiteFooter } from '@/components/layout/SiteFooter';

/** Light marketing footer (used by {@link MarketingPageShell}). */
export function MarketingFooter() {
  return <SiteFooter variant="light" />;
}
